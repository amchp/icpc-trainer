# Keep Electron as a local wrapper

Electron is a local wrapper over the web app and local backend, not the primary application runtime. The application stays web-first and browser-runnable, while Electron adds a desktop shell for local use and later distribution.
