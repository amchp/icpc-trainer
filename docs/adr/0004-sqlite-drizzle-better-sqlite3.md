# Use SQLite through Drizzle and better-sqlite3

We use SQLite via `better-sqlite3`, with Drizzle as the schema and query layer. This supports local-first persistence while avoiding raw SQL spread across the application.
