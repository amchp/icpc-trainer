# Use a pnpm monorepo

We use a pnpm monorepo with separate web, server, desktop, and shared packages. This preserves clean boundaries similar to T3 Code while keeping this starter simpler than adopting its custom build orchestration.
