export const dataStructures = {
  loading: "Loading the Data Structures guide…",
  roadmap: "Learning roadmap",
  eyebrow: "Data Structures · C++17",
  title: "Let the problem ask for the structure.",
  subtitle: "Meet a constraint, predict the operation you need, then trace the C++ tool that satisfies it.",
  heroPrompt: "How many cells are in a 1,000,000,000 × 1,000,000,000 grid?",
  heroAnswer: "10¹⁸ does not fit in int",
  sidebar: {
    label: "Data Structures guide sections",
    progress: "Section {{current}} of {{total}}"
  },
  sections: {
    numeric: "Exact numeric range",
    vector: "Sorted sequences",
    stack: "Nested state",
    queue: "Arrival order",
    set: "Unique values",
    map: "Keyed state",
    ranges: "Custom structure design",
    reference: "Container reference"
  },
  lesson: {
    challenge: "The challenge",
    input: "What you receive",
    output: "What you must produce",
    sample: "Small sample",
    walkthrough: "How the answer is obtained",
    thinkEyebrow: "Your turn",
    thinkTitle: "Try to solve it before continuing",
    thinkIntro: "Stop here. Work out your own algorithm and write a first draft. Open the tool lesson only after you have made an honest attempt.",
    toolbox: "Tool lesson",
    revealToolkit: "Learn the tool",
    hideToolkit: "Hide the tool lesson",
    tool: "What the tool does",
    operations: "Operations and cost",
    connectionEyebrow: "If you are still stuck",
    revealConnection: "Show the full solution explanation",
    hideConnection: "Hide the full solution explanation",
    connectionTitle: "Solution",
    connectionDescription: "Read the idea, follow how it processes the sample, and use the correctness argument to check your own approach.",
    connectionComplexity: "Cost",
    solutionDemoEyebrow: "Change the input",
    solutionDemoTitle: "Walk through the algorithm",
    solutionDemoDescription: "Edit the input, then play the animation or move through it one step at a time. The walkthrough shows state changes without revealing implementation code.",
    solutionDemoState: "Resulting state",
    animation: {
      controls: "Animation controls",
      play: "Play animation",
      pause: "Pause animation",
      previous: "Previous step",
      next: "Next step",
      reset: "Reset animation",
      progress: "Step {{current}} of {{total}}",
      progressLabel: "Walkthrough progress"
    }
  },
  cost: {
    amortizedConstant: "O(1) amortized"
  },
  numeric: {
    source: "Original warm-up · numeric representation",
    title: "How many cells fit in the grid?",
    summary: "A grid has a number of rows and columns. Every row contains exactly one cell for each column. You receive both dimensions and must calculate how many cells the entire grid contains, even when the dimensions are enormous.",
    sample: "Input: rows = 3, columns = 4 → Output: 12",
    walkthrough: "There are 3 rows and each one contains 4 cells. The total is therefore 4 + 4 + 4 = 12, which is the same as calculating 3 × 4.",
    input: "Two positive whole numbers, rows and columns. Each can be as large as 10⁹.",
    output: "Their exact product, with every digit preserved.",
    gridLabel: "3 rows by 4 columns: 12 painted cells",
    toolTitle: "Integer and floating-point number types",
    explanation: "A numeric type decides which values can be represented and whether those values are exact. Integer expressions are evaluated using the operand types at that moment. Floating-point types trade exact integer precision for a much wider magnitude range.",
    operations: {
      arithmetic: "Performs arithmetic on values of the same type. With fixed-width primitive types, each operation takes constant time; the result still has to fit the operand type.",
      cast: "Converts value to type T before it is used in an expression. A conversion cannot recover information already lost to an earlier overflow."
    },
    reasoning: {
      model: "The only state is the two dimensions and their product. Before choosing a type, use the constraints to calculate the largest intermediate value: 10⁹ × 10⁹ = 10¹⁸. That value must fit while the multiplication happens, not only in the variable that will receive the result.",
      process: "First compare 10¹⁸ with the available ranges: a typical int reaches about 2 × 10⁹ and is too small; a typical long long reaches about 9 × 10¹⁸ and is large enough. Choose an exact 64-bit integer type for both dimensions before multiplying. Then both 3 × 4 and 10⁹ × 10⁹ are evaluated in the wide type and produce 12 and 10¹⁸ respectively.",
      why: "Integer arithmetic preserves every digit while every result remains inside the type’s range. Because the operands are already long long, the multiplication happens in 64 bits. Storing an int multiplication in a larger variable afterward would not help: the overflow has already happened by then.",
      complexity: "One multiplication takes O(1) time because the program performs a fixed number of primitive operations. It stores only rows, columns, and their product, so it uses O(1) space."
    },
    demoEyebrow: "Failure experiment",
    demoTitle: "The same expression passes a small test and fails at scale",
    demoDescription: "Both examples compile. The first result is trustworthy because 600 fits in int. In the second, the int multiplication overflows before cout sees the value; signed overflow is undefined behavior, so the output cannot be trusted.",
    demoSmall: "Small values",
    demoLarge: "Boundary values",
    demoFailure: "Expected 10¹⁸ · int result is not trustworthy",
    bits: {
      choose: "Try your own multiplication",
      leftOperand: "First factor",
      rightOperand: "Second factor",
      inputHint: "Use whole numbers from 0 to 1e9.",
      invalidInput: "Enter a whole number from 0 to 1e9 in each field.",
      result: "Mathematical result",
      capacity: "Positive-value capacity of a typical signed int",
      operandsTitle: "The input bits determine the product size",
      inputBits: "{{count}} input bits",
      productBits: "{{count}} product bits",
      bits: "bits",
      growth: "When integers are multiplied, the product may need approximately the sum of both inputs’ bit widths.",
      available: "Value bits available:",
      required: "Value bits required:",
      fits: "The value fits.",
      overflow: "{{count}} more value bits are needed.",
      fitsExplanation: "{{product}} is at most 2³¹−1, so it fits in the 31 positive-value bits of a typical signed int.",
      overflowExplanation: "{{product}} needs {{count}} value bits. A typical signed int has only 31 positive-value bits, so the mathematical result lies outside its range.",
      smallExplanation: "600 is below 2³¹, so the 31 positive-value bits of a typical signed int are enough.",
      largeExplanation: "10¹⁸ is close to 2⁶⁰. A typical signed int only has 31 positive-value bits, so the mathematical result lies far outside its range."
    },
    types: {
      title: "C++ number types are promises about range and precision",
      description: "Integer types preserve exact whole numbers inside their range. Floating-point types cover much larger magnitudes by storing a limited number of significant digits. The language specifies minimum guarantees; exact widths can depend on the platform.",
      keyword: "Keyword",
      family: "Family",
      bits: "Typical bits",
      power: "Power-of-two range",
      approximation: "Easy approximation",
      integer: "Signed integer",
      unsigned: "Unsigned integer",
      floating: "Floating point",
      shortApprox: "about ±32 thousand",
      intApprox: "about ±2 × 10⁹",
      longLongApprox: "about ±9 × 10¹⁸",
      unsignedApprox: "0 to about 4 × 10⁹",
      floatApprox: "tiny values to about 10³⁸",
      doubleApprox: "tiny values to about 10³⁰⁸",
      note: "Use sizeof(T) and numeric_limits<T> when the exact implementation matters. For contest constraints, compare the largest possible intermediate value—not only the final variable—with the type’s range."
    }
  },
  vector: {
    source: "Codeforces 706B · Interesting Drink",
    title: "How many shops can sell within each budget?",
    summary: "The same drink is sold at a different price in every shop. Several customers then tell you how much money they have. For each budget, report how many shops can sell the drink to that customer; a shop counts when its price is less than or equal to the budget.",
    sample: "Input: prices [3, 10, 8, 6], budgets [1, 6, 9, 10] → Output: [0, 2, 3, 4]",
    walkthrough: "A budget of 1 cannot afford any shop. A budget of 6 can afford prices 3 and 6. A budget of 9 also includes price 8. A budget of 10 includes all four shops. The answers are therefore 0, 2, 3, and 4.",
    input: "A list of up to 100,000 prices, each at most 100,000, followed by up to 100,000 budget queries; a budget can reach 10⁹.",
    output: "For each budget, the number of prices that are less than or equal to it.",
    toolTitle: "vector, sorting, and positions",
    explanation: "vector stores a resizable sequence in contiguous memory. Indexed access reads one position directly, push_back grows the sequence at its end, and sort rearranges a selected range in ascending order. The next sections build from these basics to iterator positions.",
    operations: {
      index: "Reads or changes the element stored exactly at index. The index must be inside the vector.",
      pushBack: "Adds one value after the current last element. Some insertions reserve a larger memory block and copy or move the existing elements.",
      sort: "Rearranges the range from smallest to largest and modifies the original vector.",
      lowerBound: "On an already sorted range, returns the first position whose value is greater than or equal to the target.",
      upperBound: "On an already sorted range, returns the first position whose value is strictly greater than the target."
    },
    reasoning: {
      model: "Sort the shop prices once. Sorting [3, 10, 8, 6] gives [3, 6, 8, 10]. For any budget, every affordable price then forms one continuous prefix: shops the customer can afford come first, followed by prices that are too large.",
      process: "For each budget, use upper_bound to obtain the iterator to the first strictly greater price. In [3, 6, 8, 10], budget 1 places it at position 0; budget 6 at position 2; budget 9 at position 3; and budget 10 at end(), position 4. Subtracting begin() gives 0, 2, 3, and 4 shops. Repeat only this search for each query; do not sort again.",
      why: "By definition, upper_bound leaves every price less than or equal to the budget before its iterator, including repeated prices equal to the limit. Only greater prices remain from that iterator onward. Therefore the distance from begin() is exactly the affordable prefix length and exactly the requested count.",
      complexity: "Sorting n prices once costs O(n log n). Each of q budgets uses an O(log n) binary search, for O(n log n + q log n) total time and O(n) storage. Scanning every shop for every customer would cost O(nq), which is too much when both can be 100,000."
    },
    solutionDemo: {
      pricesInput: "Shop prices",
      queriesInput: "Budget queries",
      pricesHint: "Enter 1 to 8 positive prices separated by commas.",
      queriesHint: "Enter 1 to 8 budgets separated by commas.",
      invalid: "Use comma-separated whole numbers inside the problem constraints.",
      sortPhase: "Sort once",
      queryPhase: "Answer each query",
      original: "Original order. This visual builds the sorted result from left to right; C++ sort may move elements internally in a different sequence.",
      extract: "Move {{value}} into the next sorted position.",
      sortComplete: "The prices are sorted. Every affordable group will now be one prefix.",
      query: "For budget {{query}}, the first greater price is at position {{position}}, so {{count}} shops are affordable.",
      remaining: "Not placed yet",
      sorted: "Sorted prices",
      answers: "Answers"
    },
    iterators: {
      eyebrow: "Before binary search",
      title: "An iterator is a movable position inside a container",
      description: "An iterator does not store the element itself. It identifies a position so C++ algorithms can work with a range from begin() up to, but not including, end(). Vector iterators can move and measure distances like positions on a number line.",
      visualLabel: "Vector showing begin, a current iterator, and end",
      noValue: "no value",
      endWarning: "end() marks the position immediately after the final element. It is useful as a boundary and “not found” result, but dereferencing *end() is invalid.",
      begin: "Returns an iterator to the first element. For an empty vector, begin() equals end().",
      end: "Returns the boundary immediately after the final element; it does not point to a stored value.",
      dereference: "Reads or modifies the element at the iterator’s current position. The iterator must point to a valid element.",
      advance: "Moves the iterator to the next position. Do not advance beyond end().",
      retreat: "Moves the iterator to the previous position. Do not retreat before begin().",
      distance: "For vector iterators, subtraction gives the number of positions between them—often the index of it.",
      demoTitle: "Move the iterator yourself",
      demoDescription: "The iterator starts at begin(). Use ++it and --it to move one position at a time; dereferencing reads the value at the highlighted position.",
      controls: "Iterator movement controls",
      movePrevious: "Move the iterator one position left with --it",
      moveNext: "Move the iterator one position right with ++it",
      reset: "Return to begin",
      current: "it = begin() + {{index}}, so *it = {{value}}",
      atEnd: "it == end(); this position has no value and must not be dereferenced."
    },
    lab: {
      sortTitle: "First: sort a vector",
      sortDescription: "sort(begin, end) rearranges the selected range in ascending order. It modifies the vector itself.",
      boundsTitle: "Then: locate boundaries in sorted data",
      boundsDescription: "Both functions return iterators—positions, not counts. The range must already be sorted with the same ordering.",
      explorerLabel: "Interactive sorted-vector boundary explorer",
      explorerEyebrow: "Try several targets",
      explorerTitle: "Where do lower_bound and upper_bound land?",
      chooseTarget: "Choose a target value",
      iteratorResultTitle: "The search returns an iterator, not the answer count",
      iteratorResultDescription: "lower_bound and upper_bound place an iterator at a boundary. Choose the boundary that matches <, ≤, ≥, or >, then subtract begin() or subtract the iterator from end() to count the side you need.",
      countBefore: "number of values before it",
      countAfter: "number of values from it to the end",
      lowerMeaning: "First position containing a value greater than or equal to {{target}}.",
      upperMeaning: "First position containing a value strictly greater than {{target}}.",
      less: "lower_bound − begin counts values < target",
      lessEqual: "upper_bound − begin counts values ≤ target",
      greaterEqual: "end − lower_bound counts values ≥ target",
      greater: "end − upper_bound counts values > target"
    },
    link: "Official Codeforces 706B"
  },
  stack: {
    source: "LeetCode 20 · Valid Parentheses",
    title: "Is this a valid bracket sequence?",
    summary: "A string contains parentheses (), brackets [], and braces {}. Decide whether every closing symbol matches the correct opening symbol and whether pairs close in the proper nesting order. No opening symbol may remain unmatched, and no closing symbol may appear without a partner.",
    sample: "Input: ([{}]) → Output: valid. Input: ([)] → Output: invalid",
    walkthrough: "In ([{}]), {} closes first, then [], and finally (); that order respects the nesting. In ([)], the ) tries to close while the most recent opening symbol is [, so the string is invalid.",
    input: "A string of 1 to 10,000 characters containing only (), [], and {}.",
    output: "Whether the complete string is correctly paired and nested.",
    toolTitle: "stack: access one end only",
    explanation: "A stack follows last in, first out (LIFO). Values enter and leave through the same end, called the top. Earlier values stay hidden until every value above them is removed.",
    reasoning: {
      model: "Use the stack to store only opening symbols that still need a partner. Its top represents the most recent unmatched opening, which is the only one the next closing symbol may complete without breaking the nesting.",
      process: "Scan from left to right. For ([{}]), reading (, [, { leaves those three symbols in the stack with { on top. Reading } checks that top is { and removes it; ] removes [; and ) removes (. If a closer sees an empty stack or a different symbol on top, stop: the sequence is invalid. After the final character, the stack must also be empty.",
      why: "Proper nesting always closes the pair opened most recently, exactly LIFO order. Comparing with top detects the wrong closing type; empty detects a closer without an opening; and requiring an empty stack at the end detects openings that were never closed. Those cover every way the sequence can be invalid.",
      complexity: "Each of the n symbols is examined once, and each opening is pushed and popped at most once. Total time is O(n). A string containing only openings can leave n symbols in the stack, so the worst-case additional space is O(n)."
    },
    operations: {
      empty: "Reports whether the stack contains no values. Check it before top or pop.",
      push: "Adds value to the top; it becomes the next value top will return.",
      top: "Reads the top value without removing it. Calling it on an empty stack is invalid.",
      pop: "Removes the top value and returns nothing. Read top first if the value is needed.",
      size: "Returns the number of values currently stored."
    },
    solutionDemo: {
      sequence: "Bracket sequence",
      valid: "Valid sequence",
      invalid: "Invalid sequence",
      empty: "The stack finishes empty.",
      pending: "Unclosed symbols remain: {{symbols}}",
      unexpected: "There is no opening symbol available for {{symbol}}.",
      mismatch: "Expected {{expected}} but received {{received}}.",
      required: "Enter at least one bracket.",
      stack: "Pending openings",
      scanPhase: "Read left to right",
      start: "No symbols processed yet. The stack starts empty.",
      pushStep: "Push {{symbol}} onto the stack because it opens a new pair.",
      popStep: "{{symbol}} closes {{opening}}, so remove that opening from the top."
    },
    visual: {
      eyebrow: "Mental model",
      title: "A stack works like a pile of books",
      description: "You can only place or remove a book from the top. To reach a book below, you must first remove every book sitting above it.",
      label: "Pile of books with the accessible book at the top",
      topBook: "Dynamic programming",
      middleBook: "Graphs",
      bottomBook: "Basic algorithms",
      push: "Places a new book above every existing book.",
      pop: "Removes only the book currently on top."
    },
    link: "Official LeetCode 20"
  },
  queue: {
    source: "LeetCode 933 · Number of Recent Calls",
    title: "How many requests are still recent?",
    summary: "A server receives ping(t) calls in strictly increasing time order. After adding the new request, report how many requests happened during the last 3,000 milliseconds, including both ends of the time window. Requests older than that window no longer count.",
    sample: "Calls: ping(1), ping(100), ping(3001), ping(3002) → Answers: 1, 2, 3, 3",
    walkthrough: "At t = 1 there is one recent request. At t = 100 both requests remain recent. At t = 3001 the window begins at 1, so all three remain. At t = 3002 the window begins at 2: the request at 1 expires, while 100, 3001, and 3002 remain, so the answer is 3.",
    input: "At most 10,000 calls to ping(t). Each t is between 1 and 10⁹ and is strictly greater than the previous time.",
    output: "For every ping(t), the number of stored request times inside the inclusive interval [t − 3000, t].",
    toolTitle: "queue: enter at the back, leave at the front",
    explanation: "A queue follows first in, first out (FIFO). New values join at the back, while inspection and removal happen at the front. This preserves arrival order. In C++, queue is an adapter backed by deque by default.",
    reasoning: {
      model: "Keep a queue of times that still belong to the latest 3,000-millisecond window. Because every new t is greater than the previous one, times remain in arrival order: the oldest is always at front() and the newest is added at back().",
      process: "When ping(t) arrives, push t and compute the inclusive start t − 3000. After ping(1), ping(100), and ping(3001), the queue is [1, 100, 3001]. At ping(3002), the window starts at 2: push 3002, see that front() is 1, and remove it; the new front 100 is valid. The queue [100, 3001, 3002] has size 3, which is the answer.",
      why: "Expired requests form one prefix because times arrive sorted. While front() is less than the window start, removing it deletes exactly one expired request. When that loop stops, the front is valid and every later element is even newer; the current ping is also present. The queue contains all and only times in [t − 3000, t].",
      complexity: "Each timestamp enters once and leaves at most once. Although one call may remove several entries, those removals never repeat: across n calls the total work is O(n), or O(1) amortized per ping. The queue may hold O(n) times in the worst case."
    },
    operations: {
      empty: "Reports whether no values are waiting. Check it before front, back, or pop.",
      push: "Adds value at the back of the queue.",
      front: "Reads the oldest waiting value without removing it.",
      back: "Reads the newest waiting value without removing it.",
      pop: "Removes the front value and returns nothing.",
      size: "Returns the number of values currently waiting."
    },
    solutionDemo: {
      pingsInput: "Ping times",
      pingsHint: "Enter 1 to 8 strictly increasing times separated by commas.",
      processPhase: "Process pings in arrival order",
      start: "No pings processed yet. The queue starts empty.",
      keepStep: "Add {{time}}. Every queued request is at least {{start}}, so keep all {{count}}.",
      removeStep: "Add {{time}}. Remove {{removed}} because it is older than {{start}}; {{count}} requests remain.",
      window: "Active window: [{{start}}, {{end}}]",
      queue: "Queue",
      answers: "Answers",
      invalid: "Times must be whole numbers in strictly increasing order."
    },
    deque: {
      eyebrow: "Next tool",
      title: "deque: a sequence open at both ends",
      description: "A deque resembles a line that can also be managed from the back. It can add, inspect, and remove values at either end, and it also supports indexed access.",
      visualLabel: "Deque with access at the front and back",
      middleNote: "Inserting or erasing at a middle position is still O(n), because nearby elements must move. A deque is special at its ends, not in its middle.",
      operations: {
        pushFront: "Adds value before the element currently at the front.",
        pushBack: "Adds value after the element currently at the back.",
        ends: "Reads the values at both ends without removing them.",
        popFront: "Removes the element at the front.",
        popBack: "Removes the element at the back.",
        index: "Reads or changes the element stored at index."
      }
    },
    link: "Official LeetCode 933"
  },
  set: {
    source: "Codeforces 443A · Anton and Letters",
    title: "How many distinct letters appear?",
    summary: "The input represents a set written with braces, commas, and spaces, such as {a, b, a, c}. Only lowercase letters are data; every other character is formatting. Count how many different letters appear. A repeated letter still counts only once.",
    sample: "Input: {a, b, a, c} → Output: 3 distinct letters",
    walkthrough: "Ignore braces, commas, and spaces. The letters are a, b, a, and c. Removing the repeated a leaves {a, b, c}, so the answer is 3.",
    input: "One formatted line of at most 1,000 characters containing lowercase English letters, braces, commas, and spaces.",
    output: "The number of different lowercase letters in the line.",
    toolTitle: "set: sorted unique values",
    explanation: "set stores at most one copy of each value and keeps its values sorted. Inserting a duplicate leaves the container unchanged. The ordering lets you iterate from smallest to largest and locate boundaries with lower_bound and upper_bound.",
    reasoning: {
      model: "Use a set to represent lowercase letters that have already appeared. It does not represent every character in the line: braces, commas, and spaces are formatting and must be ignored.",
      process: "Read one character at a time and call insert only for characters between 'a' and 'z'. In {a, b, a, c}, insert a to get {a}; insert b to get {a, b}; the second a changes nothing because it already exists; then insert c to get {a, b, c}. At the end, size() returns 3.",
      why: "Every valid letter is offered to the set, so no distinct letter can be missing. Since set keeps at most one copy of each value, a repeated letter never increases its size. Together, those properties make the final entries correspond one-to-one with the distinct letters in the line.",
      complexity: "There are n characters. Each set insertion costs O(log k), where k never exceeds 26 letters, so total time is O(n log 26), effectively O(n) for this fixed alphabet. Space is bounded by O(26), or O(1). An unordered_set would also take O(n) expected time."
    },
    operations: {
      insert: "Adds the value if it is absent; inserting a duplicate changes nothing.",
      count: "Returns 1 when the value exists and 0 when it does not.",
      lowerBound: "Returns an iterator pointing to the first value greater than or equal to the target, or end() when none exists.",
      upperBound: "Returns an iterator pointing to the first value strictly greater than the target, or end() when none exists.",
      erase: "Removes the value if present.",
      size: "Returns the number of distinct stored values."
    },
    solutionDemo: {
      text: "Formatted set",
      unique: "Distinct letters kept by the set",
      count_one: "{{count}} distinct letter",
      count_other: "{{count}} distinct letters",
      processPhase: "Read one character",
      queryPhase: "Read the answer",
      answer: "Distinct-letter count",
      start: "No characters processed yet. The set starts empty.",
      ignoreStep: "Ignore {{symbol}} because it is formatting.",
      insertStep: "Insert {{symbol}} into the set.",
      duplicateStep: "{{symbol}} is already present, so the set does not change."
    },
    variants: {
      eyebrow: "Same uniqueness, different guarantees",
      title: "set and unordered_set are not interchangeable everywhere",
      description: "Both store unique values. The important choice is whether you need ordering and boundary searches, or only a quick membership check.",
      ordered: "Keeps values sorted. You can iterate from smallest to largest and use lower_bound or upper_bound.",
      orderedCost: "insert / count / erase / boundaries → O(log n)",
      unordered: "Uses a hash table. It keeps no sorted order and provides no lower_bound or upper_bound.",
      unorderedCost: "insert / count / erase → O(1) average · O(n) worst case"
    },
    link: "Official Codeforces 443A"
  },
  map: {
    source: "Codeforces 4C · Registration System",
    title: "What suffix belongs to each username?",
    summary: "A system receives username requests in order. When a name has never been used, it registers that name and replies OK. When the name already exists, it must suggest the same name followed by the number of times it had appeared before.",
    sample: "Input: abacaba, acaba, abacaba, abacaba → Output: OK, OK, abacaba1, abacaba2",
    walkthrough: "The first abacaba is new, so the system replies OK and remembers one occurrence. On its next request it has appeared once, so the suggestion is abacaba1. On the third occurrence the suggestion becomes abacaba2. acaba keeps its own independent count.",
    input: "Up to 100,000 username requests in arrival order; each name contains 1 to 32 lowercase letters.",
    output: "OK for a new name, otherwise that name plus its next numeric suffix.",
    toolTitle: "map and unordered_map: values addressed by keys",
    explanation: "A map associates each unique key with one value. map keeps keys sorted; unordered_map uses hashing and does not promise iteration order. Looking up one key never changes the values belonging to other keys.",
    reasoning: {
      model: "Associate every username with the number of times it has already been requested. The invariant is exact: immediately before a request, the stored value for that name is the suffix to use if it is repeated. Every name owns an independent counter.",
      process: "Process requests in order. The first abacaba is absent: print OK and store abacaba → 1. The first acaba also prints OK and stores acaba → 1. When abacaba returns, its count is 1: print abacaba1 and change it to 2. The next repetition prints abacaba2 and changes it to 3. Looking up acaba does not change abacaba.",
      why: "After each request, a name's counter equals the number of processed appearances. By induction, a new name becomes 1 after printing OK; if it previously had k appearances, the algorithm prints exactly suffix k and advances the counter to k + 1. No suffix repeats, and different names cannot interfere.",
      complexity: "With map, finding or updating a key costs O(log m), where m is the number of distinct names; n requests therefore cost O(n log n) time and O(n) space. With unordered_map, each operation is O(1) expected and the total is O(n) expected, with a hash-table worst case of O(n²)."
    },
    operations: {
      index: "Reads the value for key; if the key is absent, it also creates a default value.",
      emplace: "Tries to insert key and value together. If key already exists, the map keeps its current value.",
      count: "Returns 1 when key already exists and 0 when it does not, without creating a new entry.",
      lowerBound: "Returns an iterator pointing to the first entry whose key is greater than or equal to key, or end() when none exists.",
      upperBound: "Returns an iterator pointing to the first entry whose key is strictly greater than key, or end() when none exists.",
      erase: "Removes the pair belonging to key.",
      size: "Returns the number of stored keys."
    },
    solutionDemo: {
      requestsInput: "Username requests",
      requestsHint: "Enter 1 to 8 lowercase names separated by commas.",
      invalid: "Every request must contain 1 to 32 lowercase letters.",
      processPhase: "Process requests in order",
      start: "No requests processed yet. The map and answer list start empty.",
      newStep: "{{name}} is new: answer OK and store count 1.",
      repeatStep: "{{name}} has count {{previous}}: answer {{answer}}, then store count {{next}}.",
      counts: "Stored counts",
      answers: "Answers"
    },
    variants: {
      eyebrow: "Same key-value model, different guarantees",
      title: "map and unordered_map choose between order and hashing",
      description: "Both associate one value with each unique key and support indexing, count, erase, and size. The difference is whether keys stay sorted.",
      ordered: "Keeps keys sorted. This enables ordered iteration, lower_bound, and upper_bound.",
      orderedCost: "index / count / erase / boundaries → O(log n)",
      unordered: "Uses a hash table. It does not preserve sorted order and has no lower_bound or upper_bound.",
      unorderedCost: "index / count / erase → O(1) average · O(n) worst case"
    },
    link: "Official Codeforces 4C"
  },
  ranges: {
    source: "LeetCode 155 · Min Stack",
    title: "How can a stack return its minimum in O(1)?",
    summary: "Design a MinStack object that behaves like a regular stack and can also return its smallest stored value. It must support push, pop, top, and getMin, with every operation finishing in constant time. You are defining a reusable structure and its public operations, not merely processing one fixed list.",
    sample: "Operations: push(5), push(2), push(4), getMin(), pop(), getMin(), pop(), getMin() → Answers: 2, 2, 5",
    walkthrough: "After pushing 5, the minimum is 5. Pushing 2 changes it to 2; pushing 4 leaves it at 2. Removing 4 still leaves 2 as the minimum. Removing 2 exposes 5 again.",
    input: "A sequence of at most 30,000 calls to push, pop, top, and getMin. Values fit in a signed 32-bit integer, and pop, top, and getMin are called only when the structure is not empty.",
    output: "For every top() call, return the newest stored value. For every getMin() call, return the smallest value currently stored.",
    toolTitle: "Designing your own structure from rules",
    explanation: "A custom data structure combines several variables and containers behind clearly named operations. Before writing code, define what state it stores, which condition must always remain true—its invariant—and what each public operation does. The structure may use existing tools such as vector or stack internally without exposing those details to its caller.",
    reasoning: {
      model: "Every stack level must remember the inserted value and the minimum of the whole stack through that level. Think of a level as (value, minimum so far), or use two synchronized stacks: one for values and one for minima. The second fact is a stored summary, not a future search.",
      process: "Follow the sample. push(5) stores (5, 5). push(2) compares 2 with the previous minimum 5 and stores (2, 2). push(4) stores (4, 2), because 2 is still smaller. getMin reads 2 at the top. pop removes (4, 2), so the minimum remains 2; the next pop removes (2, 2) and reveals (5, 5), so getMin returns 5 again.",
      why: "The minimum stored at each level is an invariant: it is exactly the smallest value from the bottom through that point. push preserves it by comparing only the new value with the previous summary. pop performs no recalculation, because removing the top automatically reveals the summary that already belongs to the remaining stack.",
      complexity: "push performs one comparison and insertion; pop removes one level; top and getMin read the top level. Each operation does a constant amount of work and costs O(1). Storing one extra minimum per level changes only a constant factor and keeps total space O(n)."
    },
    struct: {
      eyebrow: "C++ syntax",
      title: "A struct keeps related state and operations together",
      description: "Define a struct once, then create variables of that new type. Members are public by default: fields store state, and methods describe the actions that can change or read it. This neutral counter demonstrates the syntax without implementing MinStack.",
      fieldsTitle: "Fields hold state",
      fields: "value belongs to every Counter instance and starts at zero.",
      methodsTitle: "Methods act on that state",
      methods: "add changes the stored value; get reads it without modifying the instance.",
      instanceTitle: "An instance uses the type",
      instance: "Counter visits creates one independent object, then dot notation calls its methods.",
      addOperation: "Changes this Counter instance’s stored value by amount.",
      getOperation: "Returns the stored value without changing the instance."
    },
    solutionDemo: {
      operationsInput: "MinStack operations",
      operationsHint: "Enter 1 to 10 comma-separated calls such as push(5), getMin(), pop(), or top().",
      invalid: "Use valid operations and call pop, top, or getMin only while the stack contains a value.",
      processPhase: "Update the custom stack",
      queryPhase: "Read without changing the stack",
      start: "The custom stack starts empty.",
      pushStep: "Push {{value}} and store ({{value}}, {{minimum}}) at the new top.",
      minimumStep: "The second value at the top is {{minimum}}, so getMin returns it immediately.",
      topStep: "The first value in the top pair is {{value}}, so top returns it immediately.",
      popStep: "Pop {{value}}. The revealed top already stores the restored minimum {{minimum}}.",
      popLastStep: "Pop {{value}}. The custom stack is now empty.",
      stack: "Stored (value, minimum) pairs",
      answers: "Query answers",
      top: "top"
    },
    blueprint: {
      eyebrow: "Blueprint before code",
      title: "Four decisions for designing the structure",
      description: "Use this checklist whenever several values and actions belong together. It describes the design process without choosing the state or algorithm for the anchor problem.",
      stateTitle: "Choose the minimum state",
      state: "List only the fields an object must remember between method calls. Each field should have one clear meaning.",
      invariantTitle: "Write the invariant",
      invariant: "Write one sentence that must remain true after construction and after every public method finishes.",
      operationsTitle: "Name the operations",
      operations: "Name methods after the actions a caller understands, and define each method’s input, output, and effect on the fields.",
      resultTitle: "Define the query",
      result: "Decide which methods only inspect state and mark them const; keep representation details private to the type’s interface."
    },
    link: "Official LeetCode 155"
  },
  reference: {
    eyebrow: "After the problems",
    title: "Container operation reference",
    description: "Use the narrowest structure whose guarantees match the operations the problem repeats.",
    summaryTitle: "What each structure stores and the operations you will reach for",
    summaryDescription: "Use these cards as a syntax map. The table below them compares the cost guarantees.",
    commonOperations: "Common {{container}} operations",
    orderedOnly: "* lower_bound is available on the ordered set and map variants, not their unordered hash variants.",
    complexityTitle: "Complexity comparison",
    summaries: {
      array: "A fixed-size contiguous sequence. Choose it when the number of elements is known at compile time.",
      vector: "A resizable contiguous sequence with fast indexed access and efficient insertion at the back.",
      stack: "A last-in, first-out adapter that exposes only the newest stored value.",
      queue: "A first-in, first-out adapter that preserves arrival order.",
      deque: "A resizable sequence with constant-time operations at both the front and the back.",
      set: "A collection of unique values. Ordered set supports sorted traversal and boundaries; unordered_set prioritizes average lookup speed.",
      map: "A collection that associates each unique key with one value. Ordered map supports key boundaries; unordered_map uses hashing.",
      struct: "A custom type that groups related fields and methods under one name.",
      fields: "fields",
      methods: "methods",
      instance: "Name variable"
    },
    container: "Container",
    access: "Random access",
    search: "Search",
    insert: "Insert",
    erase: "Erase",
    note: "Guarantee",
    average: "avg.",
    worst: "worst",
    notAvailable: "N/A",
    sequenceNote: "Fixed contiguous sequence",
    vectorNote: "Dynamic contiguous sequence",
    adapterNote: "Adapter end operation",
    dequeNote: "Both-ended sequence",
    orderedNote: "Sorted keys",
    hashedNote: "Hashed keys; no order",
    vectorInsert: "O(1) amortized at back / O(n) in middle",
    vectorErase: "O(1) at back / O(n) in middle",
    endOperation: "O(1) at the supported ends"
  },
  progress: {
    saveError: "Progress was not saved",
    saveErrorDescription: "The guide is still available. Try again when the connection recovers.",
    updateError: "Progress could not be updated",
    updateErrorDescription: "Your previous guide status is unchanged.",
    completed: "Guide completed",
    completedDescription: "Data Structures now counts toward your roadmap.",
    inProgress: "Guide marked in progress",
    inProgressDescription: "You can complete it again whenever you are ready."
  },
  finish: {
    eyebrow: "Seven problems, one decision habit",
    title: "Name the operation before the container.",
    description: "You can revisit every problem, thinking prompt, and concept toolbox. Only this guide status is saved.",
    markComplete: "Mark as completed",
    markProgress: "Return to in progress",
    back: "Back to roadmap"
  },
  simulator: {
    eyebrow: "Container lab",
    title: "Try operations on {{name}}",
    label: "Interactive {{name}} simulator",
    code: "Initialization and executed code",
    state: "Container state",
    output: "Last query result",
    iteratorLabel: "iterator",
    commandPreview: "C++ line preview",
    run: "Run operation",
    reset: "Reset",
    examples: "Choose an operation",
    hint: "Only the highlighted number boxes inside parentheses or brackets are editable. Press Enter or Run operation when the line is ready.",
    arguments: {
      value: "Number",
      index: "Index number",
      key: "Key number",
      mappedValue: "Value number"
    },
    field: "Object state",
    valueField: "value",
    empty: "{{name}} is empty. Run an insertion operation to create its first value.",
    names: {
      vector: "vector",
      stack: "stack",
      queue: "queue",
      deque: "deque",
      set: "set",
      map: "map",
      struct: "a Counter struct"
    },
    markers: {
      top: "top",
      front: "front",
      back: "back"
    },
    errors: {
      empty: "That operation is invalid while the container is empty.",
      index: "That index is outside the container.",
      capacity: "This visual lab holds at most 10 values. Remove one or reset it.",
      integer: "Enter a whole number that fits in a signed 32-bit int.",
      unsupported: "Choose one of the operations shown above."
    }
  }
} as const;
