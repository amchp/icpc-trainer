export const binarySearch = {
  loading: "Loading the Binary Search guide…",
  roadmap: "Learning roadmap",
  eyebrow: "Binary Search · C++17",
  title: "Protect the condition. Cut the range.",
  subtitle: "Binary Search is not a memorized loop. It is a promise: two exclusive bounds keep opposite answers, and every question removes half of what remains.",
  heroNoteTitle: "One invariant, two directions",
  heroNote: "False → true finds the first true. True → false finds the last true. Change the condition—not the discipline.",
  conceptsLabel: "Binary Search concepts",
  concepts: { condition: "Condition", sentinels: "Sentinels", halve: "Halve", verify: "Verify" },
  sidebar: { label: "Binary Search guide sections", progress: "Section {{current}} of {{total}}" },
  sections: {
    recognize: "When search applies",
    first: "Find the first occurrence",
    closest: "Find the closest value",
    numeric: "Search a numeric domain",
    bad: "First Bad Version",
    answer: "Search on the answer",
    practice: "Practice"
  },
  challenge: {
    constraints: "Constraints",
    sample: "Example",
    source: "Open original problem",
    problemStage: "Problem",
    attempt: "Before revealing the tool, name the yes/no condition and predict which side a true answer should keep.",
    attemptStage: "Quick attempt",
    reveal: "Learn the tool",
    hide: "Hide the tool",
    applicationPrompt: "Now apply the same invariant without reading solution code.",
    applicationReveal: "Show the problem connection",
    applicationHide: "Hide the problem connection",
    toolStage: "Tool",
    applicationStage: "Application"
  },
  controls: {
    values: "Sorted values",
    target: "Target",
    run: "Build trace",
    previous: "Previous step",
    next: "Next step",
    play: "Play",
    pause: "Pause",
    reset: "Reset",
    step: "Step {{current}} of {{total}}",
    result: "Result",
    probes: "{{count}} condition checks",
    invalidList: "Enter at most 12 integers separated by commas or spaces.",
    unsorted: "The values must be sorted in non-decreasing order.",
    emptyClosest: "Closest-value search needs at least one value.",
    range: "Unresolved interval",
    condition: "Condition at mid",
    trueValue: "true",
    falseValue: "false",
    sentinel: "sentinel",
    noSteps: "The answer is already determined by the two exclusive bounds.",
    reducedMotion: "Autoplay is unavailable because reduced motion is enabled; manual steps remain available."
  },
  recognize: {
    storyLabel: "The sorting-table story",
    imageAlt: "A teal dinosaur holds a new rose cube beside a row of objects arranged from smallest to largest.",
    intro: "The dinosaur already arranged every object from smallest to largest. A new cube belongs before the first object that is at least as large. A linear scan asks every object; Binary Search turns that order into one condition.",
    valuesLabel: "Object sizes",
    targetLabel: "New cube size",
    linear: "Linear scan",
    binary: "Binary Search",
    condition: "condition(i): values[i] ≥ {{target}}",
    conditionTransitionEyebrow: "Final invariant check",
    conditionTransitionTitle: "One last reminder: answers may change only once",
    conditionTransition: "You searched arrays, real intervals, versions, and capacities. Before using Binary Search on a new problem, verify the same requirement: its yes/no condition must have one boundary.",
    insertion: "The cube belongs at position {{index}}.",
    compare: "{{linear}} linear checks versus {{binary}} binary checks.",
    conditionTitle: "Can this condition be searched?",
    conditionHelp: "Toggle this longer sequence. Binary Search needs at most one change of answer; every extra change breaks the searchable boundary.",
    pattern: {
      falseTrue: "Valid: false → true. Search for the first true.",
      trueFalse: "Valid: true → false. Search for the last true.",
      constantFalse: "Valid boundary case: the condition is always false.",
      constantTrue: "Valid boundary case: the condition is always true.",
      nonMonotone: "Not searchable with one Binary Search: the answer changes more than once."
    },
    counterexample: "Restore the expanded counterexample with several answer changes.",
    toggle: "Toggle condition at position {{index}}"
  },
  tool: {
    titleFalseTrue: "First-true exclusive-sentinel template",
    titleTrueFalse: "Last-true exclusive-sentinel template",
    titleContinuous: "Continuous Binary Search on double",
    falseTrueRule: "left is known false; right is known true. A true midpoint replaces right.",
    trueFalseRule: "left is known true; right is known false. A true midpoint replaces left.",
    continuousRule: "left² ≤ x is known true and right² > x is known false. Halve this real interval until its width is at most epsilon; the midpoint of the final interval approximates the answer.",
    codeLabel: "C++ synchronized with this trace",
    codeFirstOccurrence: "int left = -1;                 // values[left] < target\nint right = values.size();      // virtual values[right] >= target\nwhile (right - left > 1) {\n  int mid = left + (right - left) / 2;\n  bool answer = values[mid] >= target;\n  if (answer) {\n    right = mid;\n  } else {\n    left = mid;\n  }\n}\nreturn right;                    // first value >= target",
    codeClosestLast: "int left = -1;                 // virtual values[left] <= target\nint right = values.size();      // virtual values[right] > target\nwhile (right - left > 1) {\n  int mid = left + (right - left) / 2;\n  bool answer = values[mid] <= target;\n  if (answer) {\n    left = mid;\n  } else {\n    right = mid;\n  }\n}\nreturn left;                     // last value <= target, or -1",
    codeClosestFirst: "int left = -1;                 // virtual values[left] <= target\nint right = values.size();      // virtual values[right] > target\nwhile (right - left > 1) {\n  int mid = left + (right - left) / 2;\n  bool answer = values[mid] > target;\n  if (answer) {\n    right = mid;\n  } else {\n    left = mid;\n  }\n}\nreturn right;                    // first value > target",
    codeNumeric: "double left = 0.0;                 // left² <= x\ndouble right = max(1.0, x + 1.0); // right² > x\nwhile (right - left > 1e-3) {    // precision for this trace\n  double mid = left + (right - left) / 2.0;\n  bool answer = mid * mid <= x;\n  if (answer) {\n    left = mid;\n  } else {\n    right = mid;\n  }\n}\nreturn (left + right) / 2.0;       // approximation",
    codeBad: "int left = 0;                  // known good version\nint right = n;                  // known bad version\nwhile (right - left > 1) {\n  int mid = left + (right - left) / 2;\n  bool answer = isBadVersion(mid);\n  if (answer) {\n    right = mid;\n  } else {\n    left = mid;\n  }\n}\nreturn right;                    // first bad version",
    codeMagic: "int left = 0;                  // canMake(left) is true\nint right = upperBound;         // canMake(right) is false\nwhile (right - left > 1) {\n  int mid = left + (right - left) / 2;\n  bool answer = canMake(mid);\n  if (answer) {\n    left = mid;\n  } else {\n    right = mid;\n  }\n}\nreturn left;                     // maximum feasible answer",
    activeCodeLine: "Running line {{line}}",
    visualizerLabel: "Binary Search visualizer",
    visualKinds: { array: "Array visualization", numberLine: "Number-line visualization" },
    visualizerHelp: "Each probe is deliberately split into three animation steps: calculate mid, evaluate the condition, then move exactly one pointer and cross out the impossible side.",
    continuousVisualizerHelp: "This number line represents real values, not array indices. Each probe calculates a double midpoint, evaluates mid² ≤ x, and moves one endpoint.",
    phaseLabel: "Probe phases",
    phases: {
      calculate: "Calculate mid",
      evaluate: "Evaluate",
      move: "Move pointer"
    },
    probe: "Probe {{probe}}",
    calculate: "Keep left and right fixed. Calculate the new midpoint from left = {{left}} and right = {{right}}; mid becomes {{mid}}.",
    evaluate: "Ask condition({{mid}}). Its answer is {{result}}; the pointers have not moved yet.",
    move: "Because the answer is {{result}}, move {{bound}} to mid = {{mid}}. The crossed-out side can no longer contain the boundary.",
    calculateContinuous: "Keep both endpoints fixed. The midpoint between {{left}} and {{right}} is {{mid}}.",
    evaluateContinuous: "Test mid² ≤ x at mid = {{mid}}. The answer is {{result}}.",
    moveContinuous: "Move {{bound}} to mid = {{mid}} and cross out the impossible continuous range.",
    pointers: {
      left: "Left pointer",
      mid: "Middle pointer",
      right: "Right pointer"
    },
    answer: "answer",
    rangeAria: "Binary Search range with left at {{left}}, mid at {{mid}}, and right at {{right}}.",
    boundary: "Boundary answer: {{value}}",
    boundaryWithPointer: "{{pointer}} = {{value}} is the answer.",
    moved: "{{bound}} moves to {{mid}} because the condition is {{result}}."
  },
  first: {
    eyebrow: "Sorted-array boundary",
    title: "Return the first copy of the target",
    description: "Given a sorted array that may contain duplicates, return the first index whose value equals target, or -1 if target is absent.",
    constraints: "0 to 12 values in this lab; values and target are safe integers. The original LeetCode problem uses unique values.",
    sample: "values = [1, 2, 2, 4, 4, 4, 4, 6, 7, 7, 12, 20], target = 7 → index 8",
    toolTitle: "Turn equality into a monotone condition",
    toolText: "Equality alone is not monotone. Ask values[i] >= target, find the first true position, and only then verify equality.",
    applicationTitle: "Trace the first occurrence",
    found: "The first target is at index {{index}}.",
    missing: "The boundary does not hold target, so the answer is -1.",
    verify: "Final verification: values[{{index}}] {{comparison}} target."
  },
  closest: {
    eyebrow: "Two neighboring boundaries",
    title: "Find the closest value",
    description: "Find the sorted-array value closest to x. If two values are equally close, choose the smaller one.",
    constraints: "1 to 12 sorted safe integers; duplicates are allowed.",
    sample: "values = [3, 5, 10, 13, 18, 25], x = 15 → 13",
    toolTitle: "Search once from each direction",
    toolText: "Find the last value <= x and the first value > x. Those are the only candidates that can be closest.",
    applicationTitle: "Compare the surviving neighbors",
    leftCandidate: "Left candidate",
    rightCandidate: "Right candidate",
    missingCandidate: "none",
    answer: "{{value}} is closest to {{target}}.",
    tie: "Equal distance: the smaller value wins."
  },
  numeric: {
    eyebrow: "Continuous ordered domain",
    title: "Approximate square root with double",
    description: "Approximate sqrt(x) by Binary Searching a real interval. The answer is not an array index or an integer boundary: it is a double inside an interval that keeps shrinking.",
    constraints: "x is a finite double from 0 through 10¹². Epsilon is from 10⁻¹² through 10⁻¹, or use 1 through 100 fixed iterations.",
    sample: "x = 10.0 → sqrt(x) ≈ 3.16227766017",
    toolTitle: "Binary Search a real interval",
    toolText: "Start with left² ≤ x and right² > x. Calculate a double midpoint, keep the half containing sqrt(x), and stop by interval width or a fixed iteration count.",
    applicationTitle: "Control precision on a double interval",
    mode: "Stopping rule",
    epsilonMode: "Stop by epsilon",
    iterationsMode: "Fixed iterations",
    x: "Value x",
    epsilon: "Epsilon",
    iterations: "Iterations",
    approximation: "Approximation",
    absoluteError: "Absolute error",
    width: "Current interval width",
    toolResult: "sqrt(10.0) ≈ {{value}}; the remaining interval is narrower than 1e-3.",
    stagnated: "The midpoint can no longer change in double precision.",
    practical: "Each probe halves the interval. Epsilon expresses a target width; fixed iterations give predictable work. Both are limited by double precision.",
    invalidDouble: "Enter a finite value from 0 through 10¹².",
    invalidEpsilon: "Enter epsilon from 10⁻¹² through 10⁻¹.",
    invalidIterations: "Enter an integer from 1 through 100."
  },
  bad: {
    eyebrow: "External monotone API",
    title: "Find the First Bad Version",
    description: "Versions are numbered 1 through n. Once a version is bad, every later version is also bad. Minimize calls to isBadVersion.",
    constraints: "1 ≤ n ≤ 10⁹; exactly one first bad version exists.",
    sample: "n = 7, first bad = 4 → answer 4",
    toolTitle: "Count expensive condition calls",
    toolText: "Version 0 is the known-good sentinel and n is known bad. Each API call keeps the half that still contains the first true.",
    applicationTitle: "Inspect versions without listing them all",
    n: "Number of versions",
    firstBad: "First bad version",
    call: "isBadVersion({{version}}) → {{result}}",
    answer: "Version {{version}} is the first bad version after {{calls}} API calls.",
    invalidN: "Enter n from 1 through 1,000,000,000.",
    invalidBad: "The first bad version must be from 1 through n."
  },
  magic: {
    eyebrow: "Maximum feasible answer",
    title: "Magic Powder - 2",
    description: "Each cookie consumes several ingredients. Existing stock plus k grams of universal powder must cover every deficit. Find the maximum cookies.",
    constraints: "The original problem allows 100,000 ingredients; this readable lab accepts 1 to 12. Numeric values keep the original 10⁹ limits.",
    sample: "need = [2, 1, 4], stock = [11, 3, 16], k = 1 → 4 cookies",
    toolTitle: "Search the last feasible capacity",
    toolText: "canMake(cookies) changes from true to false once. Search [0, 2,000,000,001] and let the condition cost O(n) work inside each probe.",
    applicationTitle: "Spend powder ingredient by ingredient",
    powder: "Magic powder k",
    ingredient: "Ingredient {{index}}",
    need: "Need per cookie",
    stock: "Current stock",
    add: "Add ingredient",
    remove: "Remove ingredient",
    preset: "Example preset",
    presets: { first: "2 billion answer", second: "Not even one", third: "One powder gram", fourth: "Powder left over" },
    candidate: "Testing {{cookies}} cookies",
    required: "Required",
    deficit: "Deficit",
    used: "Powder used",
    feasible: "Feasible",
    infeasible: "Not feasible",
    answer: "Maximum cookies: {{cookies}}",
    invalidPowder: "Enter powder from 0 through 1,000,000,000.",
    invalidIngredient: "Every need and stock value must be an integer from 1 through 1,000,000,000."
  },
  practice: {
    intro: "Transfer the invariant. These links intentionally provide no solution hints.",
    minimum: "First feasible answer",
    maximum: "Last feasible answer",
    boundary: "Sorted boundary",
    interactive: "Interactive Judge",
    unranked: "Intro · unranked",
    links: {
      koko: "Koko Eating Bananas",
      median: "Maximum Median",
      scuza: "Scuza",
      ruler: "Ruler (easy version)",
      interview: "Interview",
      packing: "Packing Rectangles"
    }
  },
  finish: {
    eyebrow: "Invariant secured",
    title: "You now search conditions, not just arrays.",
    description: "Carry the two sentinels into every problem: prove their opposite answers, preserve them after each midpoint, and verify what the returned boundary means.",
    markComplete: "Mark complete",
    markProgress: "Mark in progress",
    back: "Back to roadmap"
  },
  progress: {
    saveError: "Progress was not started",
    saveErrorDescription: "You can keep reading and try again later.",
    completed: "Binary Search completed",
    completedDescription: "Binary Search now counts toward your roadmap.",
    inProgress: "Binary Search reopened",
    inProgressDescription: "The guide is marked in progress again.",
    updateError: "Progress was not updated",
    updateErrorDescription: "Your previous status is unchanged."
  }
} as const;
