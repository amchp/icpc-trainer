{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "preDeployCommand": "pnpm --filter @icpc-trainer/server db:migrate:prod",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
